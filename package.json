{
  "name": "fewd_final_project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=8081",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "surge"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
